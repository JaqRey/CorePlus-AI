# This configuration specifies the primary backend framework and the generative AI integration selected for the project architecture. 
# The presence of these libraries ensures that the environment is fully equipped to handle secure authentication, 
# cross-origin resource sharing, and large language model interactions, signifying that the system is ready for the development phase.

# Core Backend Framework
flask==3.0.3
flask-cors==4.0.1

# AI and External Integrations
google-generativeai==0.7.2

# Security and Environment Management
python-dotenv==1.0.1
bcrypt==4.1.3

# PDF Generation
reportlab==4.0.9
html2text==2024.2.26